import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'HollowSeek',
};

export default function HomePage() {
  return <HomeView />;
}
